# Examples

| Script | Shows | Needs server? |
|---|---|---|
| — | The extension has no runnable example of its own: what it does is visible only in PHPStan's output. The fixture projects under `tests/Integration/Fixtures` are the executable demonstration — `Matchers` with and without the extension, `Misuse`, `Returns` and `Wire` — and they run as part of `make build`. | no |
